apiVersion: v1
kind: Service
metadata:
  name: {{ include "html-export.fullname" . }}
  labels:
    {{- include "html-export.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "html-export.selectorLabels" . | nindent 4 }}
